<%= Rouge::Themes::Github.render(:scope => '.highlight') %>

.highlight { margin-bottom: 22px; }

td.code {
	width: 100%;
	max-width: 0px;
}

.code pre  {
	white-space: pre;
	overflow-x: auto;
	word-break: inherit;
	word-wrap: inherit;
}

pre.lineno { word-break: normal; }
